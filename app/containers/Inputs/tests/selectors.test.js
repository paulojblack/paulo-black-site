// import { fromJS } from 'immutable';
// import { makeSelectInputsDomain } from '../selectors';

// const selector = makeSelectInputsDomain();

describe('makeSelectInputsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
